{% extends "overview.twig" %}

{% block content %}
<h1>Source Browser</h1>

<div class="row">
    <div class="span3">
    {% set dir = tree %}
    {% include "directory.twig" %}
    </div>

    <div class="span9">
        <h2>{{path}}</h2>
        <ol class="source">{% autoescape false %}{{source}}{% endautoescape %}</ol>
    </div>
</div>
{% endblock %}
